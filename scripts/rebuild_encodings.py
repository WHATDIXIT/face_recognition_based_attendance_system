"""
Rebuild encodings by re-enrolling images, if you decide to change the encoding method.
For this minimal demo, encodings are created only via the web UI.
"""
print("No-op: encodings are generated via /enroll UI in this project.")
